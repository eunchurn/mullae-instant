import React from "react";
import styled from "styled-components";
import Draggable from "react-draggable"

const Postit = () => {
  return(
    <Draggable>
      <h1>Post-it Content</h1>
    </Draggable>
  )
}

export default Postit;
